<resources>
    <style name="Theme.MaxFit" parent="Theme.Material3">
        <!-- Customize your core color theme here, but most theming is in Compose! -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
